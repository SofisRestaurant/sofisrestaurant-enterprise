// =============================================================================
// PATH: src/modules/menu/constants.ts
// =============================================================================

// Stable skeleton loader IDs — avoids react/no-array-index-key
export const SKELETON_IDS = ['skeleton-a', 'skeleton-b', 'skeleton-c'] as const;

// Hard caps shared across hooks and the modal
export const MAX_QTY_HARD_CAP = 20;
export const MAX_NOTES_LENGTH = 600;